#spring:
#  datasource:
    # 메모리 DB -> 외부 DB 없이 격리된 환경에서 실행하고 끝나면 데이터 초기화 -> 테스트가 용이해짐
    # 스프링 부트는 datasource 설정이 없으면, 기본적으로 메모리 DB를 사용한다.
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # create와 동일하면서 애플리케이션 종료 시점에 다 날려버림. 메모리 모드는 다 해주긴 해서 딱히.
#
#    properties:
#      hibernate:
#        show_sql: true #sout에 출력
#       formate_sql: true
#
logging.level:
  org.hibernate.SQL: debug #logger를 통해서 출력
#  org.hibernate.orm.jdbc.bind: trace